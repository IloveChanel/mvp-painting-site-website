{
  "name": "mvp-painting-site-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --root pages/public",
    "build:css": "npx tailwindcss -i ./src/input.css -o ./pages/public/assets/css/style.css --minify",
    "build": "npm run build:css && vite build --config vite.config.js",
    "preview": "vite preview --config vite.config.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.10",
    "vite": "^5.4.0"
  }
}

  "engines": {
    "node": ">=20.19.4 <21"
  }
}